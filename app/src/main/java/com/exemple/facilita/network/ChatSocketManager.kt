/**
 * ⚠️ ARQUIVO OBSOLETO - NÃO USAR! ⚠️
 *
 * Este arquivo foi substituído pelo WebSocketManager unificado.
 *
 * Agora usamos apenas um WebSocketManager (network/WebSocketManager.kt)
 * que gerencia tanto o rastreamento quanto o chat em tempo real.
 *
 * Motivo: Evitar múltiplas conexões WebSocket e compartilhar a mesma
 * conexão para rastreamento e chat (baseado no app prestador).
 *
 * Data class ChatMessage foi movida para WebSocketManager.kt
 *
 * Para deletar este arquivo:
 * 1. Clique com botão direito nele
 * 2. Selecione "Delete"
 * 3. Confirme a exclusão
 */

// Este arquivo pode ser deletado com segurança

